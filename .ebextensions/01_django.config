option_settings:
  aws:elasticbeanstalk:application:environment:
    DJANGO_SETTINGS_MODULE: myproject.settings
    PYTHONPATH: /var/app/current/myproject
  # 靜態檔由 WhiteNoise 送，這裡不另外設 aws:elasticbeanstalk:environment:proxy:staticfiles

container_commands:
  01_collectstatic:
    command: "python myproject/manage.py collectstatic --noinput"
  # 註：資料庫 schema 已在上線前一次性匯入 RDS，故不在部署時自動 migrate
  # （避免首次建立環境、EB 尚未獲授權連 RDS 時失敗）。日後有新 migration
  # 時，改用 `eb ssh` 進機器手動跑 `python myproject/manage.py migrate`。
